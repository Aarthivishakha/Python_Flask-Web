[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "flask-web-api"
version = "1.0.0"
description = "Flask REST API starter for Python 3.13"
requires-python = ">=3.13,<3.14"
dependencies = [
  "Flask>=3.1.3,<4.0.0",
  "gunicorn>=23.0.0,<24.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.coverage.run]
branch = true
source = ["app"]

[tool.coverage.report]
show_missing = true
fail_under = 90

[tool.ruff]
target-version = "py313"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
